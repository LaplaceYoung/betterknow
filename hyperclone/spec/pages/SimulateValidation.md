# SimulateValidation

源模块：`SimulateValidation-CV2oIk6G.src.js`（default `t`）；路由 `/validate-success`。

## Endpoint
- 无；开发态支付验证外观模拟器。

## WS
- 无。

## State
- `tab=loading|error|failure`、support panel open；无持久化。

## Controls
- 右上 loading/error/failure 三个切换按钮；Try Again、Contact Support、support email 与失败明细模拟。

## Navigate
- 无显式 navigate（刷新仅重置模拟状态）。
