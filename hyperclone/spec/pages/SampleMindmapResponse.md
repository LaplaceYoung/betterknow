# SampleMindmapResponse

源模块：`SampleMindmapResponse-D5D9K8Os.src.js`（default `n`）；路由 `/sampleMindmapResponse`。

## Endpoint
- 无；使用硬编码样例课程/思维导图数据。

## WS
- 无。

## State
- 样例 `courseStructure`、选中/展开节点、视口交互状态；不持久化。

## Controls
- CourseStructureMap 的 zoom/pan、unit/lecture/session 节点、depth tags、Select/Confirm/Generate Course（展示用途）。

## Navigate
- 无显式 navigate。
