# SimulateProSuccess

源模块：`SimulateProSuccess-Do2m_abA.src.js`（default `n`）；路由 `/simulate-pro-success-purchase`。

## Endpoint
- 无；开发模拟页，不校验 Stripe。

## WS
- 无。

## State
- 本地 `showCelebration`/关闭状态；无持久化。

## Controls
- 复用 `ProSuccessCelebration`：购买 Pro 成功标题/说明、Continue/Close。

## Navigate
- 关闭/继续 `/`。
